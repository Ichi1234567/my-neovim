local g = vim.g

-- ===================================================
-- Leader keys
-- ===================================================
g.mapleader = ','

-- ===================================================
-- matchit.vim
-- ===================================================
-- g.switch_custom_definitions = {
--   -------------------------------------------------------------------------
--   -- 1. + ↔ - （修好！這次真的能動）
--   -- 使用 Lua pattern，不再用 Vimscript regex。
--   -------------------------------------------------------------------------
--   { ' + ', ' - ' },
--   -------------------------------------------------------------------------
--   -- 2. on ↔ off
--   -------------------------------------------------------------------------
--   { 'on', 'off' },
--
--   -------------------------------------------------------------------------
--   -- 3. is ↔ isnt
--   -- 加上 word-boundary 避免誤觸，例如 'this'
--   -------------------------------------------------------------------------
--   { ' is ', ' isnt ' },
--
--   -------------------------------------------------------------------------
--   -- 4. HTML 屬性切換
--   -------------------------------------------------------------------------
--   { 'id=', 'class=', 'style=' },
--
--   -------------------------------------------------------------------------
--   -- 5. <div> ↔ <span>（Lua pattern 版）
--   -------------------------------------------------------------------------
--   {
--     ['<div(.-)>(.-)</div>']  = '<span%1>%2</span>',
--     ['<span(.-)>(.-)</span>'] = '<div%1>%2</div>',
--   },
--
--   -------------------------------------------------------------------------
--   -- 6. <ol> ↔ <ul>
--   -------------------------------------------------------------------------
--   {
--     ['<ol(.-)>(.-)</ol>'] = '<ul%1>%2</ul>',
--     ['<ul(.-)>(.-)</ul>'] = '<ol%1>%2</ol>',
--   },
--
--   -------------------------------------------------------------------------
--   -- 7. snake_case ↔ camelCase
--   -- 按功能分開，避免互相干擾。
--   -------------------------------------------------------------------------
--
--   -- snake_case → camelCase
--   {
--     ['\v([a-z0-9]+)_([a-z])'] = '\\1\\u\\2',
--   },
--
--   -- camelCase → snake_case
--   {
--     ['\v([a-z0-9])([A-Z])'] = '\\1_\\l\\2',
--   },
--
--   -------------------------------------------------------------------------
--   -- 8. kebab-case ↔ snake_case
--   -------------------------------------------------------------------------
--
--   -- kebab-case → snake_case
--   -- {
--   --   ['\v([a-z0-9])\-([a-z])'] = '\\1_\\2',
--   -- },
--
--   -- -- snake_case → kebab-case
--   -- {
--   --   ['\v([a-z0-9])_([a-z])'] = '\\1-\\2',
--   -- },
--
--   -- -------------------------------------------------------------------------
--   -- -- 9. camelCase ↔ kebab-case（進階，但很好用）
--   -- -------------------------------------------------------------------------
--
--   -- -- camelCase → kebab-case（myVar → my-var）
--   -- {
--   --   ['\v([a-z0-9])([A-Z])'] = '\\1-\\l\\2',
--   -- },
--
--   -- -- kebab-case → camelCase（my-var → myVar）
--   -- {
--   --   ['\v\-([a-z])'] = '\\u\\1',
--   -- },
-- }
